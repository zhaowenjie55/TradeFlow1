# Local Models

Put local model files here for development only.

Expected BGE-M3 layout:

```text
models/
  bge-m3/
    model.onnx
    or model_quantized.onnx
    tokenizer.json
```

These files are ignored by Git.
